# Railway deployment config for the remote HTTP MCP server.
# Build from the Dockerfile; start the HTTP entrypoint (NOT stdio).

[build]
builder = "dockerfile"

[deploy]
startCommand = "node dist/http.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
