.qodo

node_modules
dist
coverage

.env
.env.local
.env.*.local

# Machine-specific MCP client config (absolute local paths). Commit .mcp.example.json instead.
.mcp.json

# Local Claude Code workspace state (memory, settings, session data)
.claude/

# Awtar/Wakilni business working files (real customer & financial data — NOT part of the MCP server)
awtar/
.soa-venv/

# macOS
.DS_Store
